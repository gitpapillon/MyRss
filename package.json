{
  "name": "rss-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "daily": "tsx scripts/daily.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.0",
    "typescript": "^5",
    "vitest": "^2.1.0"
  }
}
