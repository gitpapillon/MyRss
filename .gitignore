# dependencies
/node_modules

# typescript
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vitest / coverage
/coverage

# harness generated outputs (not source of truth)
.claude/worktrees/
phases/*/preview.txt
phases/*/step*-output.json
phases/*/idempotency.log
phases/*/secrets.log
phases/*/run.json
phases/*/log.txt

# daily briefings — Claude Code 데스크톱이 매일 생성하는 입력 MD (commit 불요)
files/*.md
!files/.gitkeep

# daily news pool — npm run collect 가 매일 생성하는 원시 RSS 수집물 (commit 불요)
files/news_*.json
logs/*.log
