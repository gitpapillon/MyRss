# dependencies
/node_modules

# typescript
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vitest / coverage
/coverage

# harness generated outputs (not source of truth)
.claude/worktrees/
phases/*/preview.txt
phases/*/step*-output.json
phases/*/idempotency.log
phases/*/secrets.log
phases/*/run.json
phases/*/log.txt

# daily briefings — Claude Code 데스크톱이 매일 생성하는 입력 MD (commit 불요)
files/*.md
!files/.gitkeep

# daily news pool — npm run collect 가 매일 생성하는 원시 RSS 수집물 (commit 불요)
files/news_*.json
logs/*.log

# dedup 런타임 상태 — 머신 로컬, commit 불요(logs/news 와 동일 취급).
# 단일 파일 규칙은 CLAUDE.md 유지. (반복 소실 진짜 원인은 tests/state.test.ts 격리
# 버그였고 RSS_STATE_DIR 로 수정함 — 이 ignore 는 관례상 별개로 유지.)
state/sent.json
